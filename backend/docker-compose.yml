version: "3.0"
services:
  redis:
    image: redis
    command: --logfile /data/redis.log
    ports: 
      - 6379:6379
  mongo:
    image: mongo
    command: --logpath /var/log/mongodb/log
    ports:
      - 27017:27017
